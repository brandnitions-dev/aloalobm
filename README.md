# AloAlo Business Model Terminal

A terminal-style business model presentation for AloAlo — powered by Motely Peers FZCO.

## 🚀 Live Demo

This project is deployed via **GitHub Pages**. 

Once you push to GitHub:
1. Go to your repository → **Settings** → **Pages**
2. Under "Build and deployment", select:
   - **Source**: Deploy from a branch
   - **Branch**: `main` (or `gh-pages`)
   - **Folder**: `/ (root)`
3. Click **Save**

Your site will be live at: `https://<username>.github.io/<repo>/`

## 📦 Quick Start (Local)

Simply open `index.html` in your browser:

```bash
# macOS
open index.html

# Windows
start index.html

# Linux
xdg-open index.html
```

Or use a local server:

```bash
# Python 3
python -m http.server 8000

# Node.js (npx)
npx serve .
```

Then visit `http://localhost:8000`

## 🔧 Development

This is a static HTML file with embedded CSS and JavaScript. No build process required.

## 📄 License

All rights reserved — Motely Peers FZCO
